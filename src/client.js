import React from 'react';
import ReactDOM from 'react-dom';
import useFetch from 'react-fetch-hook';


function App() {
    // const {isLoading, data} = useFetch('http://localhost:4001');
    // if(isLoading) {
    //     return 'Loading...';
    // }
    // return <pre>{JSON.stringify(data, null, 4)}</pre>;

    // form 1:  post part 1 instructions
    // get a array of valid/single shots, count the number of x, render on DOM
    // if needed, show the valid/single shots

    // form 2:  post part 2 instructions
    // get a array of combined valid/single shots, count the number of x, render on DOM
    // if needed, show the valid/single shots
}


ReactDOM.render(<App />, document.getElementById('app'));
